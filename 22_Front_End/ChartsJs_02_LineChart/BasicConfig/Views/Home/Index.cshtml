@model List<StatsViewModel>

<!DOCTYPE html>
<html>
<head>
    <title>Aplikacja</title>
    <meta charset="utf-8">
    <style>
        .container {
            width: 50%;
            margin-left: auto;
            margin-right: auto;
        }
    </style>
</head>
<body>
    <div>
        TEST
    </div>
    <div class="container">
        <canvas id="myChart" width="400" height="400"></canvas>
    </div>
    //https://stackoverflow.com/a/43658507
    <script type='text/javascript' src="~/src/lib/Chart.bundle.min.js"></script>
    <script>
        var ctx = document.getElementById('myChart').getContext('2d');
        var data = [0, 59, 75, 20, 20, 55, 40, 59, 75, 20, 20, 55, 40];
        var labels = ["0s", "10s", "20s", "30s", "40s", "50s", "60s", "10s", "20s", "30s", "40s", "50s", "60s"];
        var myChart = new Chart(ctx, {
            type: 'line',
            data: {
                labels: labels,
                datasets: [{
                    label: 'Car Speed',
                    data: data
                }]
            },
            options: {
                maintainAspectRatio: false,
                responsive: true,
                scales: {
                    yAxes: [{
                        ticks: {
                            beginAtZero: true
                        }
                    }]
                }
            }
        });
    </script>
</body>
</html>