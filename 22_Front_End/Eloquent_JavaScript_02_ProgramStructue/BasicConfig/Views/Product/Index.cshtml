<!DOCTYPE html>
<html>
<head>
    <title>Aplikacja</title>
    <meta charset="utf-8">
</head>
<body class="container">
    <div>
        TEST
    </div>
    <script>
        /*
        let ten = 10;
        console.log(ten * ten);
        // → 100

        let mood = "light";
        console.log(mood);
        // → light
        mood = "dark";
        console.log(mood);
        // → dark

        let luigisDebt = 140;
        luigisDebt = luigisDebt - 35;
        console.log(luigisDebt);
        // → 105

        let iks;
        console.log(iks);
        //undefined

        const greeting = "Hello ";
        console.log(greeting + name);
        // → Hello Ayda

        const dupa = "dupa";
        //dupa = "cipa";
        console.log(dupa);
        //Uncaught TypeError: invalid assignment to const 'dupa'

        console.log(Math.min(2, 4) + 100);
        // → 102

        let theNumber = Number(prompt("Pick a number"));
        console.log("Your number is the square root of " +
            theNumber * theNumber);

        let no = Number(prompt("Pick a number"));
        if (!Number.isNaN(no)) {
            console.log("Your number is the square root of " +
                no * no);
        }

        let theNumber = Number(prompt("Pick a number"));
        //if number
        if (!Number.isNaN(theNumber)) {
            console.log("Your number is the square root of " +
                theNumber * theNumber);
        } else {
            console.log("Hey. Why didn't you give me a number?");
        }

        //else
        let num = Number(prompt("Pick a number"));
        if (num < 10) {
            console.log("Small");
        } else if (num < 100) {
            console.log("Medium");
        } else {
            console.log("Large");
        }

        //while
        let number = 0;
        while (number <= 12) {
            console.log(number);
            number = number + 2;
        }
        // → 0
        // → 2
        // … etcetera

        //2^10 (2 to the 10th power).
        let result = 1;
        let counter = 0;
        while (counter < 10) {
            result = result * 2;
            counter = counter + 1;
        }
        console.log(result);
        // → 1024

        //do
        let yourName;
        do {
            yourName = prompt("Who are you?");
        } while (!yourName);
        console.log(yourName);

        //for
        for (let number = 0; number <= 12; number = number + 2) {
            console.log(number);
        }
        // → 0
        // → 2
        // … etcetera

        //for2
        let result = 1;
        for (let counter = 0; counter < 10; counter = counter + 1) {
            result = result * 2;
        }
        console.log(result);
        // → 1024

        //break
        for (let current = 20; ; current = current + 1) {
            if (current % 7 == 0) {
                console.log(current);
                break;
            }
        }
        // → 21

        //switch
        switch (prompt("What is the weather like?")) {
            case "rainy":
                console.log("Remember to bring an umbrella.");
                break;
            case "sunny":
                console.log("Dress lightly.");
            case "cloudy":
                console.log("Go outside.");
                break;
            default:
                console.log("Unknown weather type!");
                break;
        }
        */

        //exercise 1
        for (i = 1; i < 8; i++) {
            let line = '';
            for (j = 0; j < i; j++) {
                line = line + '#';
            }
            console.log(line);
        }

        //exercise 2
        for (i = 1; i < 100; i++) {
            if (i % 3 === 0 && i % 5 === 0) {
                console.log('FizzBuzz');
            } else if (i % 3 === 0) {
                console.log('Fizz');
            } else if (i % 5 === 0) {
                console.log('Buzz');
            } else {
                console.log(i);
            }
        }

        //exercise 3
        let num = Number(prompt("Pick a number"));
        if (!Number.isNaN(num)) {
            for (i = 0; i < num; i++) {
                let line = '';
                for (j = 0; j < num; j++) {
                    if (i % 2 === 0) {
                        if (j % 2 === 0) {
                            line = line + ' ';
                        } else {
                            line = line + '#';
                        }
                    } else {
                        if (j % 2 === 0) {
                            line = line + '#';
                        } else {
                            line = line + ' ';
                        }
                    }
                }
                console.log(line);
            }
        } else {
            console.log("Hey. Why didn't you give me a number?");
        }

    </script>
</body>
</html>